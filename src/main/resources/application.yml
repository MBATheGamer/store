spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    open-in-view: false
    # Only use for development purpose
    # hibernate:
    #   ddl-auto: create
